appId: com.tasktrees-electron.app
productName: TaskTrees
directories:
  buildResources: electron/build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron/*'
  - '!ios/*'
  - '!android/*'
  - '!functions/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.electron.json}'
  - '!capacitor.config.ts'
  - '!firebase.json'
  - '!vite.config.ts'
asarUnpack:
  - resources/**
win:
  executableName: TaskTrees
  icon: resources/icon.ico
nsis:
  artifactName: ${productName}-${version}-setup_win_${arch}.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always
  include: electron/build/installer.nsh
mac:
  icon: resources/icon.icns
  entitlementsInherit: electron/build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: true
  target:
    - target: dmg
      arch: universal
dmg:
  artifactName: ${productName}-${version}_mac_universal.${ext}
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: false
publish:
  provider: generic
  url: https://example.com/auto-updates
electronDownload:
  mirror: https://npmmirror.com/mirrors/electron/
